#include <cassert>
#include <string>
#include <iostream>
#include <limits>
#include "hashCerrado.cpp"

using namespace std;

int main()
{
    int n;
    cin >> n;


    for(int i = 0; i < n; i++){
        string operacion;
        cin >> operacion;

        if(operacion == "PUT"){

        }
        if(operacion == "GET"){

        }
        if(operacion == "REMOVE"){

        }
        if(operacion == "CONTAINS"){

        }
        if(operacion == "COUNT_DOMAIN"){

        }
        if(operacion == "LIST_DOMAIN"){

        }
        if(operacion == "CLEAR_DOMAIN"){

        }
        if(operacion == "SIZE"){

        }
        if(operacion == "CLEAR"){

        }
    }


}